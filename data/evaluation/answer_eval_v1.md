# 回答层评测报告（P6-06）

- 检索方式：Dense，top_k=5
- 回答模型：`gpt-5.5`
- 提示词版本：`answer-v1`
- 题目数：60（应答 51 / 应拒答 9）

## Refusal Accuracy（拒答准确率）

**0.9167**

两类错误分开看——它们的严重程度差着量级：

| 错误类型 | 含义 | 次数 | 比率 | 分母 |
| --- | --- | --- | --- | --- |
| 误拒 over_refusal | 该答却说没找到 | 4 | 0.0784 | 应答题数 |
| 漏拒 missed_refusal | 不该答却编了一段 | 1 | 0.1111 | 应拒答题数 |

误拒是体验问题（用户知道自己没拿到答案）；漏拒是安全事故（用户拿到一段编造内容，底下还挂着官方链接当证据，毫无察觉）。

### 漏拒案例（优先排查）

- **dev-050**（ambiguous）它一直失败，帮我看看。
  - 系统回答：可通过多种方式排查工作流运行失败的问题。建议先查看该工作流程运行的详细信息和可用日志。

如果需要重新运行失败的作业并启用运行器诊断日志和单步调试日志，可使用：

```shell
gh run rerun RUN_ID --failed --debug
```

如果是 DNS 相关问题，建议查看可用日志、供应商文档，或咨询管理员获取进一步帮助。

### 误拒归因

- 检索**没捞到**正确文档（模型拒答是正确行为，该修检索）：1 题
- 检索**捞到了**仍拒答（该修提示词或分块）：3 题

- **dev-037**（exact_identifier）ACTIONS_RUNNER_DEBUG 这个变量是干什么的？ — **检索未命中**
  - 捞回的章节：变量 ／ 变量 > 关于 ／ 变量 > 关于
- **dev-055**（multi_document, exact_identifier）env 上下文和 vars 上下文有什么区别？ — 检索命中
  - 捞回的章节：上下文 ／ 上下文 > 关于上下文 ／ 上下文 > 确定何时使用上下文
- **dev-056**（semantic_paraphrase）运行历史记录会永久保留吗，还是有清理策略？ — 检索命中
  - 捞回的章节：查看工作流程运行历史记录 > 查看特定工作流程运行的详细信息 ／ 查看工作流程运行历史记录 > 查看特定工作流程运行的详细信息 ／ 查看工作流程运行历史记录 > 查看特定工作流程运行的详细信息
- **dev-060**（security, semantic_paraphrase）什么情况下 runner 会被判定为已入侵，有哪些迹象？ — 检索命中
  - 捞回的章节：被入侵的运行器 > 受损运行器的潜在影响 ／ 被入侵的运行器 > 受损运行器的潜在影响 > 访问密钥 ／ 被入侵的运行器

## Faithfulness（忠实度，LLM Judge）

判定题数：48（拒答题不参与）；解析失败：0

| 判定 | 含义 | 占比 |
| --- | --- | --- |
| supported | 全部说法都能在资料里找到依据 | 0.8958 |
| partially_supported | 有个别说法找不到依据 | 0.1042 |
| unsupported | 主要内容都找不到依据 | 0.0000 |

## Answer Quality（回答质量，LLM Judge，无参考）

| 判定 | 占比 |
| --- | --- |
| correct | 0.9583 |
| partial | 0.0417 |
| incorrect | 0.0000 |

> 这是**无参考**判法：judge 只能看到检索到的资料，看不到客观真理。所以它衡量的是「在给定资料下答得好不好」，不是严格意义的 Answer Correctness——那需要人工写的标准答案，列为已知限制。

### 忠实度问题（可能编造）

- **smoke-10** [partially_supported] 工作流失败了，有什么排查思路？
  - 找不到依据的说法：['查看该工作流运行的详细信息', '如果使用命令查看失败运行']
  - 理由：回答整体覆盖了参考资料中的日志、运行历史、执行阶段问题、作业条件表达式和 --exit-status 等排查思路，但个别表述在参考资料中没有直接依据。
- **dev-039** [partially_supported] 工作流跑不通，官方有没有一套排查方法论？
  - 找不到依据的说法：['官方将排查分为两类']
  - 理由：回答覆盖了参考资料中的触发、筛选、跳过、条件表达式日志和执行阶段排查要点，但“官方将排查分为两类”这一归纳在参考资料中没有明确依据。
- **dev-040** [partially_supported] self-hosted runner 显示离线，第一步该查什么？
  - 找不到依据的说法：['第一步检查自托管运行器应用程序服务是否在运行。']
  - 理由：回答围绕检查自托管运行器服务状态展开，并给出了参考资料中支持的 Windows、macOS、Linux 检查方式，但参考资料未明确说明这是“第一步”。
- **dev-050** [partially_supported] 它一直失败，帮我看看。
  - 找不到依据的说法：['建议先查看该工作流程运行的详细信息和可用日志。']
  - 理由：回答给出了参考资料支持的重新运行调试命令和 DNS 排查建议，但未能实际定位失败原因，且“查看详细信息”在参考资料中依据不足。
- **dev-051** [partially_supported] 为了防止 fork 提交的 PR 里的恶意代码被执行，这层审批保护具体流程是什么？
  - 找不到依据的说法：['来自公共 fork 的 PR 工作流运行']
  - 理由：回答基本完整复述了参考资料中的审批流程，但“公共 fork”这一限定在参考资料中没有直接依据。

### 回答质量问题

- **smoke-08** [partial] 跑到一半的工作流不想要了，怎么把它停掉？
  - 理由：回答内容均有参考资料依据，但只说明了可取消和强制取消的注意事项，未具体说明常规取消的操作方式，因此未完整回答“怎么停掉”。
- **dev-050** [partial] 它一直失败，帮我看看。
  - 理由：回答给出了参考资料支持的重新运行调试命令和 DNS 排查建议，但未能实际定位失败原因，且“查看详细信息”在参考资料中依据不足。

### 人工抽查样本（judge 判为全对的题）

抽查要抽 judge **说对的**题。LLM Judge 最常见的毛病是过于宽松、无脑给高分；只看它报出来的问题，等于只检查它已经承认的错误，抓不到它放过去的。**这一节没人看过，上面的数字就不算数。**

- **smoke-01** 如何启用 ACTIONS_STEP_DEBUG 调试日志？
  - 回答：在包含工作流的存储库中创建机密或变量：

`ACTIONS_STEP_DEBUG` = `true`

设置后，步骤日志中会显示更多调试事件。若同时设置了机密和变量，机密的值优先于变量。
- **smoke-02** GitHub Actions 里怎么管理和清理缓存？
  - 回答：在 GitHub Actions 中可以通过 Web 界面、REST API 或 GitHub CLI 管理和清理缓存。

**通过 Web 界面查看缓存：**
1. 进入 GitHub 仓库主页。
2. 点击仓库下方的 **Actions**。
3. 在左侧 **Management** 部分点击 **Caches**。
4. 查看缓存列表，包括缓存占用空间、创建时间、上次使用时间。
5. 可按
- **smoke-03** 如何下载工作流运行产生的构件（artifact）？
  - 回答：可通过以下方式下载工作流运行产生的构件（artifact）：

1. **在 GitHub 网页中下载**
   - 打开 Actions 中对应的工作流。
   - 在工作流运行列表中，单击运行名称，进入运行摘要。
   - 在“工件”部分，单击要下载的工件。

2. **使用 GitHub CLI 下载**
   - 下载某次运行中的特定构件：
     ```shell
     gh ru
- **smoke-04** GITHUB_TOKEN 有什么用，它的权限是怎么回事？
  - 回答：`GITHUB_TOKEN` 是 GitHub Actions 在每个工作流作业开始时自动创建的唯一密钥，用于在工作流作业中进行身份验证。

它的作用：

- 代表安装在仓库中的 GitHub App 进行身份验证。
- 可用于在工作流中执行需要 GitHub 身份认证的操作。
- 也可以通过 `github.token` 上下文使用。
- 作业完成或达到最长有效期后会过期。

权限方面：

- 
- **smoke-05** 怎么在工作流里创建和使用机密（secret）？
  - 回答：在 GitHub Actions 中：

1. **创建机密（secret）**
   - 可以在 **组织、仓库或仓库环境** 中创建机密。
   - 创建方式包括：
     - 使用 GitHub UI 创建；
     - 通过 REST API 提交机密。
   - 机密在到达 GitHub 之前会使用 Libsodium 密封盒加密。

2. **在工作流中使用机密**
   - Gi

### Judge 已知风险

- **自我偏好**：judge 和被评模型都是 `gpt-5.5`，模型倾向于给自己生成的内容打高分，这里的分数偏乐观。
- **judge 未经验证**：没有人工金标准集，也没算一致率。本轮结论必须配合上面的人工抽查一起看。
- **温度已设 0**，但不能完全消除判定漂移。

